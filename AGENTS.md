# AGENTS.md

本文定义 ARY 项目中 Agent 协作的长期规则。业务细节以 `docs/` 下的权威文档为准。

## 工作规范

* 项目文档、计划、状态更新和协作说明优先使用中文。
* 每次开始任务前必须先读根目录 `AGENTS.md` 和 `PLAN.md`，再按任务入口阅读 `docs/` 下的相关文档。
* 不把临时进度写进长期规则；近期窗口写入 `PLAN.md`，任务状态写入 `STATUS.md`。
* 长期任务定义、依赖、验收和非目标以 `docs/ary.plan.md` 为准；近期优先级以 `PLAN.md` 为准；当前事实和风险以 `STATUS.md` 为准。

## 当前会话执行规则

* 默认由当前主会话直接完成规划、实施、验证和汇总，不创建子 Agent。
* 只有用户在当前任务中再次明确要求委派或并行 Agent 时，才允许创建子 Agent。
* 大型任务在当前会话内按互不覆盖的模块串行实施；先做定向检查，稳定后集中执行一次完整质量门。

## 风险分级与验证

实施和验证按风险等级选择流程：

| 等级 | 判断标准 | Agent 策略 | 默认验证 |
| --- | --- | --- | --- |
| L0 简单修改 | 不超过 3 个文件且不超过 100 行；仅文案、CSS、链接或明确单点修复；不涉及认证、权限、API、Server Action、Schema、数据库或安全 | 主 Agent 直接完成 | `git diff --check`；修改 TypeScript 时追加 typecheck；必要时做单页 HTTP 检查 |
| L1 局部功能 | 单一模块或小范围交互，无权限和数据边界变化 | 当前主会话直接完成 | `check:quick`，再加相关单测或一个定向 E2E |
| L2 跨页面功能 | 多个页面或共享组件发生变化，但业务边界稳定 | 当前主会话按模块串行完成 | 相关领域测试、`check:quick` 和定向 E2E；通常不跑 build 或完整 E2E |
| L3 高风险修改 | 登录、会话、权限、Server Action、Schema、迁移、数据库、生产配置、安全或跨子系统重构 | 当前主会话分批实施并集中验收 | 隔离领域测试、static、typecheck、build 和定向 E2E；稳定后完整 E2E 只集中执行一次 |
| 发布 / 里程碑 | 合并、部署、版本交付或用户明确要求完整验收 | 当前主会话执行独立验证阶段 | 完整领域测试、static、typecheck、build、production config、migration 与完整 E2E |

* L0 的文件和行数只是直接实施上限；一旦触及认证、权限、API、Server Action、Schema、数据库或安全，即使只有一行也按 L3 处理。
* 当前主会话负责检查模块边界和依赖，集中执行一次与风险等级匹配的最终验证，并确认没有覆盖用户已有修改。

## 文档入口

* 产品入口：`docs/ary-mvp.prd.md`
* 任务定义：`docs/ary.plan.md`
* 文档路由：`docs/README.md`
* 近期窗口：`PLAN.md`
* 任务看板：`STATUS.md`
* 临时任务书：`docs/registration-ca-rules-alignment.taskbook.md`（PRD-TEMP-1，已完成并入 PRD-1 基线，作为历史参考保留）

## 执行纪律

* 实施前确认目标、产出、验收口径和不做事项。
* 普通文案、CSS 和小型 L0/L1 修复不逐项更新 `PLAN.md`、`STATUS.md` 或 README；产品行为、接口、权限、Schema、数据流、安全风险、数据事故、发布风险或里程碑发生变化时集中同步一次。
* 修改长期范围、验收口径或发布要求时，同步更新对应的 `docs/` 文档。
* 重要结论必须能追溯到用户指令、仓库文件或验证结果。
* 最终回复必须列出实际运行的检查和基于风险等级主动跳过的检查；未运行的检查不得描述为通过。
