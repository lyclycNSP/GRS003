{
  "name": "ary-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --hostname 127.0.0.1",
    "build": "next build",
    "prestart": "node scripts/check-production-config.mjs",
    "start": "next start --hostname 127.0.0.1",
    "lint": "next lint",
    "prisma:generate": "prisma generate",
    "prisma:generate:sqlite": "node scripts/generate-sqlite-client.mjs",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:push": "prisma db push",
    "db:init": "npm run prisma:generate:sqlite && python scripts/init-sqlite.py",
    "seed": "node --import tsx prisma/seed.ts",
    "test": "node --import tsx tests/domain.test.ts && node --import tsx tests/work-submission.test.ts && node --import tsx tests/work-submission-domain.test.ts && node --import tsx tests/track-runtime.test.ts && node --import tsx tests/race-live-contracts.test.ts && node --import tsx tests/race-live-domain.test.ts && node --import tsx tests/race-live-projection.test.ts && node --import tsx tests/race-live-controls.test.ts && node --import tsx tests/track-calibrator-draft.test.ts && node --import tsx tests/track-publish.test.ts",
    "test:e2e": "npm run test:e2e:prepare && playwright test",
    "test:e2e:judge": "npm run test:e2e:prepare && playwright test e2e/judge.spec.ts",
    "test:e2e:public": "npm run test:e2e:prepare && playwright test e2e/public.spec.ts",
    "test:e2e:screen": "npm run test:e2e:prepare && playwright test e2e/screen.spec.ts",
    "test:e2e:rider": "npm run test:e2e:prepare && playwright test e2e/rider.spec.ts",
    "test:e2e:organizer": "npm run test:e2e:prepare && playwright test e2e/organizer.spec.ts",
    "test:e2e:admin": "npm run test:e2e:prepare && playwright test e2e/admin.spec.ts",
    "test:e2e:security": "npm run test:e2e:prepare && playwright test e2e/security.spec.ts",
    "test:e2e:prepare": "node scripts/e2e-prepare.mjs",
    "typecheck": "tsc --noEmit",
    "check:static": "node scripts/static-smoke.mjs",
    "check:production-config": "node scripts/check-production-config.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "next": "^15.3.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.15.32",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "prisma": "^6.10.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
